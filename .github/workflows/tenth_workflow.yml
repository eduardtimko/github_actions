name: "Tenth workflow"
on:
  push:
    branches:
      - main

jobs:
  first:
    runs-on: ubuntu-latest
    env:
      CUSTOM_VAR: JOB FIRST
    steps:
      name: "Print github env"
      run: |
        echo $CUSTOM_VAR
