name: "Tenth workflow"
on:
  push:
    branches:
      - main

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: "Print github env"
        run: |
          echo '${{ vars.CUSTOM_VAR }}'
